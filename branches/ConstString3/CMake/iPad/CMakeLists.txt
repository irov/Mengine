 
CMAKE_MINIMUM_REQUIRED( VERSION 2.6 )

MACRO( ADD_FILTER group_name )
	SOURCE_GROUP( ${group_name} FILES ${ARGN} )
	SET( SRC_FILES ${SRC_FILES} ${ARGN} )
	#MESSAGE( ${ARGN} )
ENDMACRO( ADD_FILTER group_name files )

# solution

PROJECT( Mengine_IPAD )

SET(MENGINE_IPAD)

INCLUDE( ../ipad_template.cmake )

SET( MENGINE_REPOSITORY ${CMAKE_CURRENT_SOURCE_DIR}/../.. )

SET(DEPENDS_LIB_DIR ${MENGINE_REPOSITORY}/xcode_depends_ipad/Release)

set(MENGINE_LIB_PREFIX ${DEPENDS_LIB_DIR}/lib)
set(MENGINE_LIB_SUFFIXD ".a")
set(MENGINE_LIB_SUFFIX ".a")

# output paths
SET( CMAKE_RUNTIME_OUTPUT_DIRECTORY ${MENGINE_REPOSITORY}/bin )

SET( CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR} )

# input and thirdparty paths
SET( THIRDPARTY_DIR ${MENGINE_REPOSITORY}/dependencies )
SET( SOURCE_DIR ${MENGINE_REPOSITORY}/src )
SET( CMAKE_TEMP_DIR ${MENGINE_REPOSITORY}/CMakeTemp/iPad )

# implementation

ADD_DEFINITIONS(-D"override = /* */")
ADD_DEFINITIONS(-DMENGE_MASTER_RELEASE)


SET( MENGE_DEPENDENCIES )
SET( APPLICATION_DEPENDENCIES )
SET( APPLICATION_LIBS )
SET( APPLICATION_LIB_PATHS )

INCLUDE_DIRECTORIES( ${SOURCE_DIR} )
INCLUDE_DIRECTORIES( ${SOURCE_DIR}/Utils )
INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/boost )

ADD_SUBDIRECTORY( ${SOURCE_DIR} ${CMAKE_TEMP_DIR}/src )

#LINK_DIRECTORIES( ${DEPENDS_LIB_DIR} ${APPLICATION_LIB_PATHS} )

foreach(path ${APPLICATION_LIB_PATHS})
	MESSAGE(${path})
endforeach(path ${APPLICATION_LIB_PATHS})

ADD_SUBDIRECTORY( ${SOURCE_DIR}/IOSApplication ${CMAKE_TEMP_DIR}/IOSApplication )

ADD_DEPENDENCIES( IOSApplication ${APPLICATION_DEPENDENCIES} )