set( MY_LIB_NAME MengeVideoCodecPlugin )

ADD_FILTER(
src
	VideoCodecPlugin.def
	VideoCodecPlugin.h
	VideoCodecPlugin.cpp
	VideoDecoder.h
	VideoDecoder.cpp
    VideoDecoderFFMPEG.cpp
	VideoDecoderFFMPEG.h
)

INCLUDE_DIRECTORIES( ${Mengine_SOURCE_DIR}/Utils 
					 ${THIRDPARTY_DIR}                 
					 ${THIRDPARTY_DIR}/ffmpeg/include
)
SET(FFMPEG_LIB_DIR  ${THIRDPARTY_DIR}/ffmpeg/lib/ )


ADD_LIBRARY( ${MY_LIB_NAME} SHARED ${SRC_FILES} )

SET(CMAKE_CXX_LINK_FLAGS "${CMAKE_CXX_LINK_FLAGS}/EXPORT:dllCreatePlugin")



SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)



SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 ${THIRDPARTY_DIR}/ffmpeg/lib/
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)


TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Logger)
TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Core)


SET(
 VIDEO_CODEC_STATIC_LIBS
 ${FFMPEG_LIB_DIR}${MENGINE_LIB_PREFIX}avcodec${MENGINE_LIB_SUFFIX}
 ${FFMPEG_LIB_DIR}${MENGINE_LIB_PREFIX}avfilter${MENGINE_LIB_SUFFIX}
 ${FFMPEG_LIB_DIR}${MENGINE_LIB_PREFIX}avformat${MENGINE_LIB_SUFFIX}
 ${FFMPEG_LIB_DIR}${MENGINE_LIB_PREFIX}avutil${MENGINE_LIB_SUFFIX}
 ${FFMPEG_LIB_DIR}${MENGINE_LIB_PREFIX}postproc${MENGINE_LIB_SUFFIX}
 ${FFMPEG_LIB_DIR}${MENGINE_LIB_PREFIX}swresample${MENGINE_LIB_SUFFIX}
 ${FFMPEG_LIB_DIR}${MENGINE_LIB_PREFIX}swscale${MENGINE_LIB_SUFFIX}
 )
TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${VIDEO_CODEC_STATIC_LIBS} )



