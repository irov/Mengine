
set( MY_LIB_NAME Box2DPhysicSystem )

ADD_FILTER(
src
	Box2DPhysicBody.cpp
	Box2DPhysicBody.h
	Box2DPhysicJoint.cpp
	Box2DPhysicJoint.h
	Box2DPhysicScaler.cpp
	Box2DPhysicScaler.h
	Box2DPhysicSystem.cpp
	Box2DPhysicSystem.h
	Box2DPhysicSystem.def
)


INCLUDE_DIRECTORIES( ${Mengine_SOURCE_DIR}/Utils )
INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/Box2D )
INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/boost )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 ${THIRDPARTY_DIR}/Box2D/Library/
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)

TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Core)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
	debug ${MENGINE_LIB_PREFIX}box2d_static${MENGINE_LIB_SUFFIXD}
	optimized ${MENGINE_LIB_PREFIX}box2d_static${MENGINE_LIB_SUFFIX}
  
  #debug ${MENGINE_LIB_PREFIX}Box2D${MENGINE_LIB_SUFFIX}
  #optimized ${MENGINE_LIB_PREFIX}Box2D${MENGINE_LIB_SUFFIX}
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)
