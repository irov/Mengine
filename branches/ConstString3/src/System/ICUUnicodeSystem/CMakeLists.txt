
set( MY_LIB_NAME ICUUnicodeSystem )

ADD_FILTER(
src
	ICUUnicodeSystem.cpp
	ICUUnicodeSystem.h
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/icu/source/common )

add_definitions(-DU_STATIC_IMPLEMENTATION -DU_SIZEOF_WCHAR_T=2)

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 debug icu_static.lib
 optimized icu_static.lib
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
