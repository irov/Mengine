
set( MY_LIB_NAME OALSoundSystem )

ADD_FILTER(
src
	OALSoundBufferBase.cpp
	OALSoundBufferBase.h
	OALSoundBuffer.cpp
	OALSoundBuffer.h
	OALSoundBufferStream.cpp
	OALSoundBufferStream.h
	OALSoundSource.cpp
	OALSoundSource.h
	SulkSystem.cpp
	SulkSystem.h
	OALSoundSystem.cpp
	OALSoundSystem.h
	OALError.cpp
	OALError.h	
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/OpenAL/include )

ADD_DEFINITIONS( -DAL_STATIC_LIB )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

IF(WIN32)
    SET( APPLICATION_LIBS
     ${APPLICATION_LIBS}
     debug ${THIRDPARTY_DIR}/OpenAL/libs/Win32/OpenAL32.lib
     optimized ${THIRDPARTY_DIR}/OpenAL/libs/Win32/OpenAL32.lib
    # debug ${MENGINE_LIB_PREFIX}openal_static${MENGINE_LIB_SUFFIXD}
    # optimized ${MENGINE_LIB_PREFIX}openal_static${MENGINE_LIB_SUFFIX}
    )

	SET( APPLICATION_LIBS
	 ${APPLICATION_LIBS}
	${MENGINE_LIB_PREFIX}WS2_32${MENGINE_LIB_POSTFIX}
	${MENGINE_LIB_PREFIX}dxguid${MENGINE_LIB_POSTFIX}
	)
	IF(MSVC)
		SET( APPLICATION_LIB_PATHS
		 ${APPLICATION_LIB_PATHS}
		 ${THIRDPARTY_DIR}/DirectX/DX8/lib
		)
	ENDIF(MSVC)
	IF(NOT MSVC)
		SET( APPLICATION_LIBS
		 ${APPLICATION_LIBS}
		${MENGINE_LIB_PREFIX}Winmm${MENGINE_LIB_POSTFIX}
		)
	ENDIF(NOT MSVC)
ENDIF(WIN32)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
