
ADD_FILTER(
src
	WindowsLayer.cpp
	WindowsLayer.h
)

ADD_LIBRARY( WindowsLayer ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 WindowsLayer
)


SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)
