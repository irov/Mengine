MENGINE_PROJECT(Arena3DNetProxy)

ADD_FILTER(src Arena3DNetProxyMain.cpp)

if(MENGINE_TARGET_WINDOWS)
    ADD_MENGINE_EXECUTABLE_WIN32_CONSOLE()
else()
    ADD_MENGINE_EXECUTABLE_UNIX()
endif()

TARGET_LINK_LIBRARIES(${PROJECT_NAME} Arena3DHeadless SocketService)
set_target_properties(${PROJECT_NAME} PROPERTIES FOLDER Game)
