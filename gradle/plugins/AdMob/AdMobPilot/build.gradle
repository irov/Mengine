apply from: rootProject.projectDir.getAbsolutePath() + '/plugins/plugin.gradle'

android {
    namespace "org.Mengine.Plugin.AdMobPilot"
}

rootProject.ext.plugin_extensions += 'org.Mengine.Plugin.AdMob.Pilot.MengineAdMobPilotExtension'

dependencies {
    implementation project(':plugins:AdMob:Core')
    implementation project(':plugins:Pilot')
}
