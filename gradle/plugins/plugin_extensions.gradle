android {
    defaultConfig {
        buildConfigField "String[]", "MENGINE_GRADLE_ANDROID_PLUGIN_EXTENSIONS", '{' + android.ext.extensions.collect {
            "\"${it}\""
        }.join(",") + '}'
    }
}