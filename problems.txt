------------------------------------------------------------------------------
12.06.07, approved by Kesha
УБИТЬ МЕМЛИКИ, УБИТЬ!
------------------------------------------------------------------------------
Про кодеки:
  Кеша: короче проблема такая:

есть код который читает анимационный формат MNG. 
  Иров: угу
  Кеша: спрашивается - ГДЕ он должен быть, КАК его оформить, должен ли ДВИЖ про него знать, или нада создать фабрику ФОРМАТОВ
вот типа такого
  Иров: у нас за прочтение отвечает ResourceUmageMNG
  Кеша: bool ResourceImageMNG::_compile()
	{
		FileDataInterface* fileData = Holder<FileEngine>::hostage()->openFile(m_fileMNG);

		assert(fileData != 0);

		mnglib::mngDesc	desc;

		readMNG(
			desc,
			(unsigned char*)fileData->getBuffer(),
			fileData->size()
			);

		Holder<FileEngine>::hostage()->closeFile(fileData);

		m_size.x = desc.width;
  Кеша: ВОТ:
		readMNG(
			desc,
			(unsigned char*)fileData->getBuffer(),
			fileData->size()
			);

  Кеша: какого хера движ должен знать про readMNG?
  Иров: хм, да проблема но думаю пока можно опустить ее
------------------------------------------------------------------------------
НЕТУ ПОИСКА ПУТИ!
------------------------------------------------------------------------------
Нету утилит для работы с анимационными форматами.
------------------------------------------------------------------------------
ПЕРЕПИСАТЬ ЗВУК НАФИГ
------------------------------------------------------------------------------
РАЗРУЛИТЬ ПРОБЛЕМУ С РЕСУРСАМИ
------------------------------------------------------------------------------
