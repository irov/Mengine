MENGINE_PROJECT(Bootstrapper)

ADD_FILTER(
Bootstrapper
    Bootstrapper.cpp
    Bootstrapper.h
)

ADD_MENGINE_LIBRARY(Bootstrapper)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} Kernel)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}stdex${MENGINE_LIB_SUFFIX})
TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}math${MENGINE_LIB_SUFFIX})

IF(MENGINE_EXTERNAL_SOURCE)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ExternalBootstrapper)
ENDIF()

IF(MENGINE_PLUGIN_ASTRALAX_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} AstralaxPlugin)
ENDIF()

IF(MENGINE_PLUGIN_DAZZLE_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} DazzlePlugin)
ENDIF()

IF(MENGINE_PLUGIN_BITMAPFONT_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} BitmapFontPlugin)
ENDIF()

IF(MENGINE_PLUGIN_NODEDEBUGRENDER_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} NodeDebugRenderPlugin)
ENDIF()

IF(MENGINE_PLUGIN_RESOURCEPREFETCHER_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ResourcePrefetcherPlugin)
ENDIF()

IF(MENGINE_PLUGIN_RESOURCECONVERT_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ResourceConvertPlugin)
ENDIF()

IF(MENGINE_PLUGIN_RESOURCEVALIDATE_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ResourceValidatePlugin)
ENDIF()

IF(MENGINE_PLUGIN_RESOURCEDEBUGGER_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ResourceDebuggerPlugin)
ENDIF()

IF(MENGINE_PLUGIN_METABUFLOADER_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} MetabufLoaderPlugin)
ENDIF()

IF(MENGINE_PLUGIN_PVRTC_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} PVRTCPlugin)
ENDIF()

IF(MENGINE_PLUGIN_ETC1_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ETC1Plugin)
ENDIF()

IF(MENGINE_PLUGIN_DDS_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} DDSPlugin)
ENDIF()

IF(MENGINE_PLUGIN_JPEG_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} JPEGPlugin)
ENDIF()

IF(MENGINE_PLUGIN_PNG_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} PNGPlugin)
ENDIF()

IF(MENGINE_PLUGIN_WEBP_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} WebPPlugin)
ENDIF()

IF(MENGINE_PLUGIN_VIDEO_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} VideoPlugin)
ENDIF()

IF(MENGINE_PLUGIN_THEORA_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} TheoraPlugin)
ENDIF()

IF(MENGINE_PLUGIN_CURL_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} cURLPlugin)
ENDIF()

IF(MENGINE_PLUGIN_SPINE_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} SpinePlugin)
ENDIF()

IF(MENGINE_PLUGIN_MOVIE_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} MoviePlugin)
ENDIF()

IF(MENGINE_PLUGIN_MOVIE1_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} Movie1Plugin)
ENDIF()

IF(MENGINE_PLUGIN_TTF_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} TTFPlugin)
ENDIF()

IF(MENGINE_PLUGIN_FE_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} FEPlugin)
ENDIF()

IF(MENGINE_PLUGIN_WIN32_SOCKET_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} Win32SocketPlugin)
ENDIF()

IF(MENGINE_PLUGIN_NODEDEBUGGER_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} NodeDebuggerPlugin)
ENDIF()

IF(MENGINE_PLUGIN_OZZ_ANIMATION_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} OzzAnimationPlugin)
ENDIF()

IF(MENGINE_PLUGIN_PYTHONFRAMEWORK_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} PythonFramework)
ENDIF()

IF(MENGINE_PLUGIN_UIFRAMEWORK_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} UIFramework)
ENDIF()

IF(MENGINE_PLUGIN_JSON_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} JSONPlugin)
ENDIF()

IF(MENGINE_PLUGIN_BOX2D_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} Box2DPlugin)
ENDIF()

IF(MENGINE_PLUGIN_TEXTUREPACKER_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} TexturepackerPlugin)
ENDIF()

IF(MENGINE_PLUGIN_GRAPHICS_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} GraphicsPlugin)
ENDIF()

IF(MENGINE_PLUGIN_SENTRY_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} SentryPlugin)
ENDIF()

IF(MENGINE_PLUGIN_STEAM_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} SteamPlugin)
ENDIF()

IF(MENGINE_PLUGIN_GOAP_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} GOAPPlugin)
ENDIF()

IF(MENGINE_PLUGIN_IMGUI_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ImGUIPlugin)
ENDIF()

IF(MENGINE_PLUGIN_CAMERADEBUGGIZMO_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} CameraDebugGizmoPlugin)
ENDIF()

IF(MENGINE_PLUGIN_WIN32_ANTIFREEZEMONITOR_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} Win32AntifreezeMonitorPlugin)
ENDIF()

IF(MENGINE_PLUGIN_ANDROID_NATIVE_KERNEL_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} AndroidNativeKernelPlugin)
ENDIF()

IF(MENGINE_PLUGIN_ANDROID_NATIVE_FACEBOOK_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} AndroidNativeFacebookPlugin)
ENDIF()

IF(MENGINE_PLUGIN_ANDROID_NATIVE_UNITYADS_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} AndroidNativeUnityAdsPlugin)
ENDIF()

IF(MENGINE_PLUGIN_ANDROID_NATIVE_ADMOB_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} AndroidNativeAdMobPlugin)
ENDIF()

IF(MENGINE_PLUGIN_ANDROID_NATIVE_DEVTODEV_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} AndroidNativeDevToDevPlugin)
ENDIF()

IF(MENGINE_PLUGIN_ANDROID_NATIVE_LINKING_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} AndroidNativeLinkingPlugin)
ENDIF()

IF(MENGINE_PLUGIN_ANDROID_NATIVE_LOCAL_NOTIFICATIONS_STATIC)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} AndroidNativeLocalNotificationsPlugin)
ENDIF()