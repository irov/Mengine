MENGINE_PROJECT(Bootstrapper)

ADD_FILTER(
Bootstrapper
    Bootstrapper.cpp
    Bootstrapper.h
)

ADD_MENGINE_LIBRARY(Bootstrapper)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} Kernel)

IF(MENGINE_EXTERNAL_BOOTSTRAPPER_NAME)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${MENGINE_EXTERNAL_BOOTSTRAPPER_NAME})
ENDIF()

FOREACH(PluginName ${APPLICATION_STATIC_PLUGINS})
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${PluginName})
ENDFOREACH()