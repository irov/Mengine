
CMAKE_MINIMUM_REQUIRED( VERSION 2.6 )

MACRO( ADD_FILTER group_name )
	SOURCE_GROUP( ${group_name} FILES ${ARGN} )
	SET( SRC_FILES ${SRC_FILES} ${ARGN} )
	#MESSAGE( ${ARGN} )
ENDMACRO( ADD_FILTER group_name files )

# solution

PROJECT( Mengine )

SET( MENGINE_REPOSITORY ${Mengine_SOURCE_DIR}/.. )
SET( MENGINE_DEBUG_POSTFIX "_d")

# output paths
SET( LIBRARY_OUTPUT_PATH ${MENGINE_REPOSITORY}/lib )

SET( EXECUTABLE_OUTPUT_PATH ${MENGINE_REPOSITORY}/bin )
SET( EXECUTABLE_OUTPUT_PATH_Release ${MENGINE_REPOSITORY}/bin )
SET( EXECUTABLE_OUTPUT_PATH_Debug ${MENGINE_REPOSITORY}/bin_d )

# input and thirdparty paths
SET( THIRDPARTY_DIR ${CARBON_REPOSITORY}/dependencies )

# implementation
SET( CMAKE_DEBUG_POSTFIX "${MENGINE_DEBUG_POSTFIX}" )

if (NOT MSVC)
	ADD_DEFINITIONS(-D'override = /* */')
endif(NOT MSVC)


SET( MENGE_APPLICATION )


SET( MENGE_DEPENDENCIES 
	Math 
	XmlParser 
)
SET( APPLICATION_DEPENDENCIES 
	Menge
	AstralaxParticleSystem
	Box2DPhysicSystem
	MengeLogSystem
	OISInputSystem
	OALSoundSystem
)

SET( WINAPPLICATION_DEPENDENCIES
	HGERenderSystem
	MengeWin32FileSystem
)

SET( MACOSXAPPLICATION_DEPENDENCIES
	OGLRenderSystem
	MengeMacOSFileSystem
)

IF( WIN32 )
	SET( MENGE_APPLICATION WinApplication )
	SET( APPLICATION_DEPENDENCIES ${APPLICATION_DEPENDENCIES} ${WINAPPLICATION_DEPENDENCIES} )
ELSE( APPLE )
	SET( MENGE_APPLICATION MacOSApplication )
	SET( APPLICATION_DEPENDENCIES ${APPLICATION_DEPENDENCIES} ${MACOSAPPLICATION_DEPENDENCIES} )
ENDIF( WIN32 )

ADD_SUBDIRECTORY( libs )

ADD_SUBDIRECTORY( Menge )
ADD_DEPENDENCIES( Menge Math ${MENGE_DEPENDENCIES} )

ADD_SUBDIRECTORY( System )

ADD_SUBDIRECTORY( ${MENGE_APPLICATION} )
ADD_DEPENDENCIES( ${MENGE_APPLICATION} ${APPLICATION_DEPENDENCIES} )