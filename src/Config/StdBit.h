#pragma once

#include "Config/Config.h"

#include <bit>

namespace Mengine
{
    namespace StdBit
    {
        using std::bit_cast;
        using std::bit_ceil;
        using std::has_single_bit;
        using std::countr_zero;
    }
}
