#pragma once

#include "Config/Config.h"

#include <utility>

namespace Mengine
{
    namespace StdUtility
    {
        using std::pair;
        using std::make_pair;
        using std::move;
        using std::forward;
    }
}
