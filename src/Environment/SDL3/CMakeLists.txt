MENGINE_PROJECT(SDL3)

ADD_FILTER(
src
    SDL3Includer.h
    SDL3MainIncluder.h
    
    SDL3PlatformServiceExtensionInterface.h
    
    SDL3MessageBoxLogger.h
    SDL3MessageBoxLogger.cpp
)

INCLUDE_DIRECTORIES(${THIRDPARTY_CONFIG_DIR}/SDL3/include/include-config-$<LOWER_CASE:$<CONFIG>>)
INCLUDE_DIRECTORIES(${THIRDPARTY_CONFIG_DIR}/SDL3/include)
INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/SDL3/include)

ADD_MENGINE_LIBRARY(Environment)

if(MENGINE_TARGET_WINDOWS)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}SDL3-static${MENGINE_LIB_SUFFIX})
endif()

if(MENGINE_TARGET_UWP)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}SDL3-static${MENGINE_LIB_SUFFIX})
endif()

if(MENGINE_TARGET_APPLE)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}SDL3${MENGINE_LIB_SUFFIX})
endif()

if(MENGINE_TARGET_UNIX)
    TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}SDL3${MENGINE_LIB_SUFFIX})
endif()