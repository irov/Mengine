if(MENGINE_PLUGIN_SIMPLEFRAMEWORK)
    ADD_SUBDIRECTORY(SimpleFramework)
endif()

if(MENGINE_BUILD_MENGINE_SCRIPT_EMBEDDED)
    if(MENGINE_PLUGIN_PYTHONFRAMEWORK)
        ADD_SUBDIRECTORY(PythonFramework)
    endif()
endif()

if(MENGINE_PLUGIN_UIFRAMEWORK)
    ADD_SUBDIRECTORY(UIFramework)
endif()

MENGINE_SUBFOLDER()