#include "ScheduleHelper.h"

namespace Mengine
{
    namespace Helper
    {
        uint32_t scheduleEvent( const ScheduleManagerInterfacePtr & _scheduler, float _delay, const LambdaScheduleEvent & _event )
        {

        }

        uint32_t scheduleTiming( const ScheduleManagerInterfacePtr & _scheduler, float _delay, const LambdaSchedulePipe & _pipe, const LambdaScheduleTiming & _timing, const LambdaScheduleEvent & _event )
        {

        }
    }
}