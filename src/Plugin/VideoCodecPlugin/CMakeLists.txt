set( MY_LIB_NAME VideoCodecPlugin )

ADD_FILTER(
src
	VideoCodecPlugin.h
	VideoCodecPlugin.cpp
    VideoCodecPlugin.def
    
    VideoDecoderTheora.cpp
    VideoDecoderTheora.h
    
#    VideoDecoderGVF.cpp
#    VideoDecoderGVF.h
    
#    VideoDecoderVPX.cpp
#    VideoDecoderVPX.h
#    VideoI420ToRGB24.h
#    VideoI420ToRGB24.cpp
#    VideoMkvReader.h
#    VideoMkvReader.cpp    
)

INCLUDE_DIRECTORIES( ${Mengine_SOURCE_DIR}/Utils )

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/libtheora/include )
INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/libogg/include )
INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/libmath/include )

#INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/libwebm )
#INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/libvpx )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

set_target_properties (${MY_LIB_NAME} PROPERTIES
    FOLDER Plugins
)

TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Logger)
TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Core)
TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Factory)
TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Codec)

TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}stdex${MENGINE_LIB_SUFFIX} )
TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}theora${MENGINE_LIB_SUFFIX} )
TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}ogg${MENGINE_LIB_SUFFIX} )
#TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}lz4${MENGINE_LIB_SUFFIX} )
#TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}webm${MENGINE_LIB_SUFFIX} )
#TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}libvpx${MENGINE_LIB_SUFFIX} )

TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${VIDEO_CODEC_STATIC_LIBS} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS} 
 PARENT_SCOPE
)

