MENGINE_PROJECT(INIPlugin)

ADD_FILTER(
src
    INIPlugin.h
    INIPlugin.def
    INIPlugin.cpp

    INIUtils.h
    INIUtils.cpp
    INIConfig.h
    INIConfig.cpp
)

INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/tinyini/include)

ADD_MENGINE_PLUGIN(MENGINE_PLUGIN_INI)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}tinyini${MENGINE_LIB_SUFFIX})

TARGET_LINK_LIBRARIES(${PROJECT_NAME} Kernel)