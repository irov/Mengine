MENGINE_PROJECT(JPEGPlugin)

ADD_FILTER(
src
    JPEGIncluder.h
    
    JPEGPlugin.h
    JPEGPlugin.def
    JPEGPlugin.cpp

    ImageDecoderJPEG.cpp
    ImageDecoderJPEG.h
    ImageEncoderJPEG.cpp
    ImageEncoderJPEG.h
)

INCLUDE_DIRECTORIES(${THIRDPARTY_CONFIG_DIR}/libjpeg)
INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/libjpeg)

ADD_MENGINE_PLUGIN(MENGINE_PLUGIN_JPEG)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}jpeg${MENGINE_LIB_SUFFIX})

TARGET_LINK_LIBRARIES(${PROJECT_NAME} Kernel)