MENGINE_PROJECT(OggVorbisPlugin)

ADD_FILTER(
src
    OggVorbisPlugin.cpp
    OggVorbisPlugin.def
    OggVorbisPlugin.h
    SoundDecoderOGGVorbis.cpp
    SoundDecoderOGGVorbis.h
)

INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/libvorbis/include)
INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/libogg/include)
    
add_definitions(-DOV_EXCLUDE_STATIC_CALLBACKS)

ADD_MENGINE_PLUGIN(MENGINE_PLUGIN_OGG)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}vorbisfile${MENGINE_LIB_SUFFIX})
TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}vorbis${MENGINE_LIB_SUFFIX})
TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}ogg${MENGINE_LIB_SUFFIX})