set(MY_LIB_NAME TheoraPlugin)

ADD_FILTER(
src
	TheoraPlugin.h
	TheoraPlugin.cpp
    TheoraPlugin.def
    
    TheoraVideoDecoder.cpp
    TheoraVideoDecoder.h
	)

INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/libtheora/include)
INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/libogg/include)
INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/libmath/include)

ADD_MENGINE_LIBRARY(Plugins)

TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Kernel)

TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}stdex${MENGINE_LIB_SUFFIX})
TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}theora${MENGINE_LIB_SUFFIX})
TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}ogg${MENGINE_LIB_SUFFIX})

TARGET_LINK_LIBRARIES(${MY_LIB_NAME} ${VIDEO_CODEC_STATIC_LIBS})

