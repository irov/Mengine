MENGINE_PROJECT(WebPPlugin)

ADD_FILTER(
src
    WebPPlugin.h
    WebPPlugin.cpp
    WebPPlugin.def

    ImageDecoderWebP.h
    ImageDecoderWebP.cpp
)
    
INCLUDE_DIRECTORIES(${THIRDPARTY_CONFIG_DIR}/libwebp)
INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/libwebp/src)

ADD_MENGINE_PLUGIN(MENGINE_PLUGIN_WEBP)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}libwebp${MENGINE_LIB_SUFFIX})

TARGET_LINK_LIBRARIES(${PROJECT_NAME} Kernel)