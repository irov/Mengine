MENGINE_PROJECT(Win32ConsoleLoggerPlugin)

ADD_FILTER(
src
    Win32ConsoleLoggerPlugin.h
    Win32ConsoleLoggerPlugin.cpp
    
    Win32ConsoleLogger.h
    Win32ConsoleLogger.cpp
)

ADD_MENGINE_PLUGIN(MENGINE_PLUGIN_WIN32_CONSOLELOGGER)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} Windows)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} Kernel)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}stdex${MENGINE_LIB_SUFFIX})
