
ADD_FILTER(
src
	Box2DPhysicBody.cpp
	Box2DPhysicBody.h
	Box2DPhysicJoint.cpp
	Box2DPhysicJoint.h
	Box2DPhysicSystem.cpp
	Box2DPhysicSystem.h
	Box2DPhysicSystem.def
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/Box2D/Include )

ADD_LIBRARY( Box2DPhysicSystem ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 Box2DPhysicSystem
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 debug ${MENGINE_LIB_PREFIX}box2d_staticd${MENGINE_LIB_SUFFIX}
 optimized ${MENGINE_LIB_PREFIX}box2d_static${MENGINE_LIB_SUFFIX}
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 ${THIRDPARTY_DIR}/box2d/${MENGINE_LIB_DIR}
)


SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
