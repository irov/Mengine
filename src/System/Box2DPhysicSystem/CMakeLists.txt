set( MY_LIB_NAME Box2DPhysicSystem )

ADD_FILTER(
src
	Box2DPhysicBody.cpp
	Box2DPhysicBody.h
    Box2DPhysicWorld.cpp
    Box2DPhysicWorld.h
	Box2DPhysicSystem.cpp
	Box2DPhysicSystem.h
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/Box2D/Box2D )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

set_target_properties (${MY_LIB_NAME} PROPERTIES
    FOLDER System
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)

TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Core)

IF( MINGW )
    SET( APPLICATION_LIBS
     ${APPLICATION_LIBS}
        ${THIRDPARTY_LIB_DIR}/libBox2D.a
    )
ELSEIF( MSVC )
    SET( APPLICATION_LIBS
     ${APPLICATION_LIBS}
        ${THIRDPARTY_LIB_DIR}/Box2D.lib
    )
ENDIF( MINGW )

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)
