set( MY_LIB_NAME CurlHttpSystem )

ADD_FILTER(
src
	CurlHttpSystem.cpp
	CurlHttpSystem.h
    
    ThreadTaskDownloadAsset.h
    ThreadTaskDownloadAsset.cpp
)

add_definitions(-DCURL_STATICLIB)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/libcurl/include )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

IF( MINGW )
    SET( APPLICATION_LIBS
     ${APPLICATION_LIBS}
        ${THIRDPARTY_DIR}/${MENGINE_LIB_DIR}/${CMAKE_BUILD_TYPE}/libcurl.a
    )
ELSEIF( MSVC )
    SET( APPLICATION_LIBS
     ${APPLICATION_LIBS}
        ${THIRDPARTY_DIR}/${MENGINE_LIB_DIR}/${CMAKE_BUILD_TYPE}/libcurl.lib
    )
ENDIF( MINGW )

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
