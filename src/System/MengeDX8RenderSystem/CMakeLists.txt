
ADD_FILTER(
src
	DX8RenderSystem.h
	DX8RenderSystem.cpp
	DX8Texture.h
	DX8Texture.cpp
	DX8RenderTexture.h
	DX8RenderTexture.cpp
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/DirectX/DX8/Include )

ADD_LIBRARY( MengeDX8RenderSystem ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 MengeDX8RenderSystem
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

if(MSVC)
SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 d3d8.lib
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 ${THIRDPARTY_DIR}/DirectX/DX8/lib
)

else(MSVC)
	SET( APPLICATION_LIBS
	 ${APPLICATION_LIBS}
	 ${MENGINE_LIB_PREFIX}d3d8${MENGINE_LIB_SUFFIX}
	)

endif(MSVC)


SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
