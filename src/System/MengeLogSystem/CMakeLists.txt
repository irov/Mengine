

ADD_FILTER(
src
	LogSystem.cpp
	LogSystem.h
	LogSystem.def
)

ADD_LIBRARY( MengeLogSystem ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 MengeLogSystem
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)