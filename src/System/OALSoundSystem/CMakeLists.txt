
ADD_FILTER(
src
	OALSoundBufferBase.cpp
	OALSoundBufferBase.h
	OALSoundBuffer.cpp
	OALSoundBuffer.h
	OALSoundBufferStream.cpp
	OALSoundBufferStream.h
	OALSoundSource.cpp
	OALSoundSource.h
	SulkSystem.cpp
	SulkSystem.h
	OALSoundSystem.cpp
	OALSoundSystem.h
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/OpenAL/Include )
IF( WIN32 )
	INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/pthreads )
	INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR} )
ENDIF( WIN32 )

ADD_LIBRARY( OALSoundSystem ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 OALSoundSystem
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 debug ${MENGINE_LIB_PREFIX}openal_staticd${MENGINE_LIB_SUFFIX}
 optimized ${MENGINE_LIB_PREFIX}openal_static${MENGINE_LIB_SUFFIX}
 debug ${MENGINE_LIB_PREFIX}pthreads_staticd${MENGINE_LIB_SUFFIX}
 optimized ${MENGINE_LIB_PREFIX}pthreads_static${MENGINE_LIB_SUFFIX}
)

IF(MSVC)
	SET( APPLICATION_LIBS
	 ${APPLICATION_LIBS}
	WS2_32.lib
	)
ELSE(MSVC)
	SET( APPLICATION_LIBS
	 ${APPLICATION_LIBS}
	${MENGINE_LIB_PREFIX}WS2_32${MENGINE_LIB_SUFFIX}
	${MENGINE_LIB_PREFIX}Winmm${MENGINE_LIB_SUFFIX}
	)
ENDIF(MSVC)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 ${THIRDPARTY_DIR}/OpenAL/${MENGINE_LIB_DIR}
 ${THIRDPARTY_DIR}/pthreads/${MENGINE_LIB_DIR}
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
