set( MY_LIB_NAME OALSoundSystem )

ADD_FILTER(
src
	OALSoundBufferBase.cpp
	OALSoundBufferBase.h
	OALSoundBufferMemory.cpp
	OALSoundBufferMemory.h
	OALSoundBufferStream.cpp
	OALSoundBufferStream.h
	OALSoundSource.cpp
	OALSoundSource.h
	OALSoundSystem.cpp
	OALSoundSystem.h
	OALSoundError.cpp
	OALSoundError.h	
)

add_definitions(-DAL_LIBTYPE_STATIC)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/openal-soft/include/AL )
INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/openal-soft/OpenAL32/include )


ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
    ${APPLICATION_LIBS}
    ${THIRDPARTY_DIR}/${MENGINE_LIB_DIR}/${CMAKE_BUILD_TYPE}/${MENGINE_LIB_PREFIX}OpenAL32${MENGINE_LIB_SUFFIX}
    )

IF(WIN32)
    SET( APPLICATION_LIBS
     ${APPLICATION_LIBS}
    Winmm${MENGINE_LIB_SUFFIX}
    )
ENDIF(WIN32)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
