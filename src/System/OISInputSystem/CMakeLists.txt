
set( MY_LIB_NAME OISInputSystem )

if( MENGE_USE_MD )
	set( MY_LIB_NAME ${MY_LIB_NAME}_md )
endif( MENGE_USE_MD )


ADD_FILTER(
src
	OISInputSystem.cpp
	OISInputSystem.h
	OISInputSystem.def
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/OIS/Includes )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 debug ${MENGINE_LIB_PREFIX}OIS_static${MENGINE_LIB_SUFFIXD}
 optimized ${MENGINE_LIB_PREFIX}OIS_static${MENGINE_LIB_SUFFIX}
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 ${THIRDPARTY_DIR}/OIS/${MENGINE_LIB_DIR}
)

IF(WIN32)
	SET( APPLICATION_LIBS
	 ${APPLICATION_LIBS}
	 ${MENGINE_LIB_PREFIX}dxguid${MENGINE_LIB_POSTFIX}
	 ${MENGINE_LIB_PREFIX}dinput8${MENGINE_LIB_POSTFIX}
	)
	IF(MSVC)
		SET( APPLICATION_LIB_PATHS
		 ${APPLICATION_LIB_PATHS}
		 ${THIRDPARTY_DIR}/DirectX/DX8/lib
		)
	ENDIF(MSVC)
ENDIF(WIN32)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
