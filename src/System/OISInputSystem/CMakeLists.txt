

ADD_FILTER(
src
	OISInputSystem.cpp
	OISInputSystem.h
	OISInputSystem.def
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/OIS/Includes )

ADD_LIBRARY( OISInputSystem ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 OISInputSystem
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 debug ${MENGINE_LIB_PREFIX}OIS_staticd${MENGINE_LIB_SUFFIX}
 optimized ${MENGINE_LIB_PREFIX}OIS_static${MENGINE_LIB_SUFFIX}
 dxguid.lib
 dinput8.lib
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 ${THIRDPARTY_DIR}/OIS/${MENGINE_LIB_DIR}
 ${THIRDPARTY_DIR}/DirectX/DX8/lib
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
