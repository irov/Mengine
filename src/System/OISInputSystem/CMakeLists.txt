

ADD_FILTER(
src
	OISInputSystem.cpp
	OISInputSystem.h
	OISInputSystem.def
)

ADD_LIBRARY( OISInputSystem ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 OISInputSystem
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 debug OIS_static_d.lib
 optimized OIS_static.lib
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)
