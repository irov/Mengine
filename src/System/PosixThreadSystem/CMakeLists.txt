
set( MY_LIB_NAME PosixThreadSystem )

if( MENGE_USE_MD )
	set( MY_LIB_NAME ${MY_LIB_NAME}_md )
endif( MENGE_USE_MD )

ADD_FILTER(
src
	PosixThreadSystem.cpp
	PosixThreadSystem.h
)

IF( WIN32 )
	INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/pthreads )
	ADD_DEFINITIONS( -DPTW32_STATIC_LIB )
ENDIF( WIN32 )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

IF(WIN32)
	SET( APPLICATION_LIBS
	 ${APPLICATION_LIBS}
	 debug ${MENGINE_LIB_PREFIX}pthreads_static${MENGINE_LIB_SUFFIXD}
	 optimized ${MENGINE_LIB_PREFIX}pthreads_static${MENGINE_LIB_SUFFIX}
	)
ENDIF(WIN32)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

IF(WIN32)
	SET( APPLICATION_LIB_PATHS
	 ${APPLICATION_LIB_PATHS}
	 ${THIRDPARTY_DIR}/pthreads/${MENGINE_LIB_DIR}
	 )
ENDIF(WIN32)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
