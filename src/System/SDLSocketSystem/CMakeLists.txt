SET( MY_LIB_NAME SDLSocketSystem )

ADD_FILTER(
src
    SDLSocket.h
    SDLSocket.cpp
    SDLSocketSystem.h
    SDLSocketSystem.cpp
)

add_definitions(-DUNICODE)

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

INCLUDE_DIRECTORIES(
    ${Mengine_SOURCE_DIR}
    ${Mengine_SOURCE_DIR}/Utils
    ${THIRDPARTY_DIR}/SDL2/include
    ${THIRDPARTY_DIR}/SDL2_net
)

SET( APPLICATION_LIBS
    ${APPLICATION_LIBS}
    ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}SDL2_net${MENGINE_LIB_SUFFIX}
    )

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_DEPENDENCIES
    ${APPLICATION_DEPENDENCIES}
    ${MY_LIB_NAME}
)


SET( APPLICATION_DEPENDENCIES
    ${APPLICATION_DEPENDENCIES}
    PARENT_SCOPE
)
