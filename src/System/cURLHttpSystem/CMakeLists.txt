set( MY_LIB_NAME cURLHttpSystem )

ADD_FILTER(
src
	cURLHttpSystem.cpp
	cURLHttpSystem.h
    
    ThreadTaskGetAsset.h
    ThreadTaskGetAsset.cpp
    ThreadTaskPostMessage.h
    ThreadTaskPostMessage.cpp
)

add_definitions(-DCURL_STATICLIB)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/libcurl/include )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
	${THIRDPARTY_LIB_DIR}/libcurl${MENGINE_LIB_SUFFIX}
)

IF(WIN32)
SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
	ws2_32.lib
)
ENDIF()

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
