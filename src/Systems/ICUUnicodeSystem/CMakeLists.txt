MENGINE_PROJECT(ICUUnicodeSystem)

ADD_FILTER(
src
	ICUUnicodeSystem.cpp
	ICUUnicodeSystem.h
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/icu/source/common )

add_definitions(-DU_STATIC_IMPLEMENTATION)

ADD_MENGINE_LIBRARY(Systems)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}icu${MENGINE_LIB_SUFFIX})