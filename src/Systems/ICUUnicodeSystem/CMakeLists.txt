
set( MY_LIB_NAME ICUUnicodeSystem )

ADD_FILTER(
src
	ICUUnicodeSystem.cpp
	ICUUnicodeSystem.h
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/icu/source/common )

add_definitions(-DU_STATIC_IMPLEMENTATION)

ADD_MENGINE_LIBRARY()

set_target_properties (${MY_LIB_NAME} PROPERTIES
    FOLDER Systems
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}icu${MENGINE_LIB_SUFFIX} 
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
