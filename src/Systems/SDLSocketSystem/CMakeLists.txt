MENGINE_PROJECT(SDLSocketSystem)

ADD_FILTER(
src
    SDLSocket.h
    SDLSocket.cpp
    SDLSocketSystem.h
    SDLSocketSystem.cpp
)

include_directories(${THIRDPARTY_CONFIG_DIR}/SDL)
INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/SDL2/include)
INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/SDL2_net)

ADD_MENGINE_LIBRARY(Systems)

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}SDL2_net${MENGINE_LIB_SUFFIX})