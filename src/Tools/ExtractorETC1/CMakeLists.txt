MENGINE_PROJECT(ExtractorETC1)

ADD_FILTER(
src
    ExtractorETC1.cpp
)

LINK_DIRECTORIES( ${CMAKE_ARCHIVE_OUTPUT_DIRECTORY} )
ADD_MENGINE_TOOL_EXECUTABLE()

TARGET_LINK_LIBRARIES(${PROJECT_NAME} Kernel)

TARGET_LINK_LIBRARIES( ${PROJECT_NAME}
    ToolUtils
    )
