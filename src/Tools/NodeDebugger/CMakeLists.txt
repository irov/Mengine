set( MY_LIB_NAME NodeDebugger )

ADD_FILTER(
src
	NodeDebuggerApp.h
    NodeDebuggerApp.cpp
	
	imgui_impl_glfw_gl3_glad.h
	imgui_impl_glfw_gl3_glad.cpp
	
	main.cpp
)

add_definitions(-DUNICODE)
add_definitions(-D_UNICODE)
add_definitions(-D_WINSOCK_DEPRECATED_NO_WARNINGS)

INCLUDE_DIRECTORIES(
	${THIRDPARTY_DIR}/libmath/include
	${THIRDPARTY_DIR}/pybind/include
	${THIRDPARTY_DIR}/glad/include
	${THIRDPARTY_DIR}/glfw3/include
    ${THIRDPARTY_DIR}/pugixml/src
    ${THIRDPARTY_DIR}/zed_net
    ${THIRDPARTY_DIR}/imgui
)

LINK_DIRECTORIES( ${CMAKE_ARCHIVE_OUTPUT_DIRECTORY} )
ADD_EXECUTABLE( ${MY_LIB_NAME} WIN32 ${SRC_FILES} )

TARGET_LINK_LIBRARIES(${MY_LIB_NAME} Kernel)

TARGET_LINK_LIBRARIES( ${MY_LIB_NAME}
	${MENGINE_SOURCE_DIR}
    ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}stdex${MENGINE_LIB_SUFFIX}
	${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}math${MENGINE_LIB_SUFFIX}
	${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}pugixml${MENGINE_LIB_SUFFIX}	
	${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}imgui${MENGINE_LIB_SUFFIX}
	${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}pybind${MENGINE_LIB_SUFFIX}	
	${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}glfw3${MENGINE_LIB_SUFFIX}
	${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}glad${MENGINE_LIB_SUFFIX}
    )

set_target_properties( ${MY_LIB_NAME} PROPERTIES LINK_FLAGS /NODEFAULTLIB:"libci.lib" )
