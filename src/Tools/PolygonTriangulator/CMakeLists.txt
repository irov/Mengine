MENGINE_PROJECT(PolygonTriangulator)

ADD_FILTER(
src
    PolygonTriangulator.cpp
)

add_definitions(-DUNICODE)
add_definitions(-D_UNICODE)

INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/Clipper2/CPP/Clipper2Lib/include)
INCLUDE_DIRECTORIES(${THIRDPARTY_DIR}/earcut/include)

LINK_DIRECTORIES(${CMAKE_ARCHIVE_OUTPUT_DIRECTORY})
ADD_MENGINE_EXECUTABLE_WIN32_CONSOLE()

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${THIRDPARTY_LIB_DIR}/${MENGINE_LIB_PREFIX}Clipper2${MENGINE_LIB_SUFFIX})

TARGET_LINK_LIBRARIES(${PROJECT_NAME} ToolUtils)