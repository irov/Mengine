
ADD_FILTER(
src
	XmlElement.cpp
	XmlElement.h
	XmlElementListener.cpp
	XmlElementListener.h
	XmlElementValueListener.h
	XmlExpatParser.cpp
	XmlExpatParser.h
	XmlParser.cpp
	XmlParser.h
	XmlParserCast.cpp
	XmlParserCast.h
	XmlParserCheckMethod.h
	XmlParserElement.cpp
	XmlParserElement.h
)

ADD_DEFINITIONS( -DXML_STATIC )

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/expat/lib )

ADD_LIBRARY( XmlParser ${SRC_FILES} )
SET( MENGE_DEPENDENCIES
 ${MENGE_DEPENDENCIES}
 XmlParser
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 debug ${MENGINE_LIB_PREFIX}expat_staticd${MENGINE_LIB_SUFFIX}
 optimized ${MENGINE_LIB_PREFIX}expat_static${MENGINE_LIB_SUFFIX}
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 ${THIRDPARTY_DIR}/expat/${MENGINE_LIB_DIR}
)
 
SET( MENGE_DEPENDENCIES
 ${MENGE_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 XmlParser
)


SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)


SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
