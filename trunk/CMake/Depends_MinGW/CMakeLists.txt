
CMAKE_MINIMUM_REQUIRED( VERSION 2.6 )

PROJECT( Depends_MinGW )

INCLUDE( ../mingw_template.cmake )

set(MENGINE_LIB_DIR "build_mingw" )

SET(MENGINE_TARGET_MINGW ON)

SET( MENGINE_REPOSITORY ${CMAKE_CURRENT_SOURCE_DIR}/../.. )
SET( THIRDPARTY_DIR ${MENGINE_REPOSITORY}/dependencies/cmake_scripts )

SET( CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR} )

SET( CMAKE_TEMP_DIR ${MENGINE_REPOSITORY}/CMakeTemp/Depends_MinGW/${CMAKE_BUILD_TYPE} )

ADD_DEFINITIONS(-D"nullptr=0")
ADD_DEFINITIONS(-DSTDEX_UNSUPPOT_NULLPTR_T)

ADD_SUBDIRECTORY( ${THIRDPARTY_DIR} ${CMAKE_TEMP_DIR} )

