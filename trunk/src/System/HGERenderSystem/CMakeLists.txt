
ADD_FILTER(
src
	graphics.cpp
	hge.h
	hge_impl.h
	system.cpp
	HGERenderFont.cpp
	HGERenderFont.h
	HGETexture.cpp
	HGETexture.h
	HGERenderSystem.cpp
	HGERenderSystem.h
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/DirectX/DX8/Include )

ADD_LIBRARY( HGERenderSystem ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 HGERenderSystem
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

if(MSVC)
SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 d3d8.lib
 d3dx8.lib 
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 ${THIRDPARTY_DIR}/DirectX/DX8/lib
)

else(MSVC)
	SET( APPLICATION_LIBS
	 ${APPLICATION_LIBS}
	 ${MENGINE_LIB_PREFIX}d3d8${MENGINE_LIB_SUFFIX}
	 ${MENGINE_LIB_PREFIX}d3dx8${MENGINE_LIB_SUFFIX}
	)

endif(MSVC)


SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
