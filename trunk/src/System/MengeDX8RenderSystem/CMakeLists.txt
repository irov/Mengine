
set( MY_LIB_NAME MengeDX8RenderSystem )

if( MENGE_USE_MT )
	set( MY_LIB_NAME ${MY_LIB_NAME}_mt )
endif( MENGE_USE_MT )

ADD_FILTER(
src
	DX8RenderSystem.h
	DX8RenderSystem.cpp
	DX8Texture.h
	DX8Texture.cpp
	DX8RenderTexture.h
	DX8RenderTexture.cpp
)

include_directories( 
${Mengine_SOURCE_DIR}/Utils
${THIRDPARTY_DIR}/DirectX/DX8/Include )
#INCLUDE_DIRECTORIES(  )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 ${MENGINE_LIB_PREFIX}d3d8${MENGINE_LIB_POSTFIX}
)

if(MSVC)

	SET( APPLICATION_LIB_PATHS
	 ${APPLICATION_LIB_PATHS}
	 ${THIRDPARTY_DIR}/DirectX/DX8/lib
	)

endif(MSVC)


SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
