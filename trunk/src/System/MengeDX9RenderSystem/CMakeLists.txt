
set( MY_LIB_NAME MengeDX9RenderSystem )

ADD_FILTER(
src
	DX9RenderSystem.h
	DX9RenderSystem.cpp
	DX9Texture.h
	DX9Texture.cpp
	DX9RenderTexture.h
	DX9RenderTexture.cpp
)

if( MSVC )
			set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} /DD3D_DEBUG_INFO")
			set(CMAKE_C_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} /DD3D_DEBUG_INFO")
else( MSVC )
			set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} -DD3D_DEBUG_INFO")
			set(CMAKE_C_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} -DD3D_DEBUG_INFO")
endif( MSVC )

include_directories( ${THIRDPARTY_DIR}/DirectX/DX9/Include )
#INCLUDE_DIRECTORIES( ${Mengine_SOURCE_DIR}/Menge )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 ${MENGINE_LIB_PREFIX}d3d9${MENGINE_LIB_POSTFIX}
)

if(MSVC)

	SET( APPLICATION_LIB_PATHS
	 ${APPLICATION_LIB_PATHS}
	 ${THIRDPARTY_DIR}/DirectX/DX9/lib
	)

endif(MSVC)


SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
