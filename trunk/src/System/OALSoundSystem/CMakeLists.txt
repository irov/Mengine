set( MY_LIB_NAME OALSoundSystem )

ADD_FILTER(
src
	OALSoundBufferBase.cpp
	OALSoundBufferBase.h
	OALSoundBuffer.cpp
	OALSoundBuffer.h
	OALSoundBufferStream.cpp
	OALSoundBufferStream.h
	OALSoundSource.cpp
	OALSoundSource.h
	OALSoundSystem.cpp
	OALSoundSystem.h
	OALError.cpp
	OALError.h	
)

INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/openalsoft/include )
INCLUDE_DIRECTORIES( ${THIRDPARTY_DIR}/openalsoft/OpenAL32/include )

ADD_LIBRARY( ${MY_LIB_NAME} ${SRC_FILES} )

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 ${MY_LIB_NAME}
)

SET( APPLICATION_DEPENDENCIES
 ${APPLICATION_DEPENDENCIES}
 PARENT_SCOPE
)

IF(WIN32)
        SET( APPLICATION_LIBS
        ${APPLICATION_LIBS}
        WS2_32.lib
        ${THIRDPARTY_DIR}/DirectX/DX8/lib/dxguid.lib
        )

    IF(MINGW)
        MESSAGE(${THIRDPARTY_DIR}/${MENGINE_LIB_DIR}/${CMAKE_BUILD_TYPE}/${MENGINE_LIB_PREFIX}OpenAL32.dll${MENGINE_LIB_SUFFIX})
        SET( APPLICATION_LIBS
        ${APPLICATION_LIBS}
        ${THIRDPARTY_DIR}/${MENGINE_LIB_DIR}/${CMAKE_BUILD_TYPE}/${MENGINE_LIB_PREFIX}OpenAL32.dll${MENGINE_LIB_SUFFIX}
        )
        
        SET( APPLICATION_LIBS
		 ${APPLICATION_LIBS}
		Winmm${MENGINE_LIB_SUFFIX}
		)
    ENDIF(MINGW)

	IF(MSVC)
        SET( APPLICATION_LIBS
        ${APPLICATION_LIBS}
        ${THIRDPARTY_DIR}/${MENGINE_LIB_DIR}/${CMAKE_BUILD_TYPE}/${MENGINE_LIB_PREFIX}OpenAL32${MENGINE_LIB_SUFFIX}
        )
	ENDIF(MSVC)
ENDIF(WIN32)

SET( APPLICATION_LIBS
 ${APPLICATION_LIBS}
 PARENT_SCOPE
)

SET( APPLICATION_LIB_PATHS
 ${APPLICATION_LIB_PATHS}
 PARENT_SCOPE
)
